import 'package:flutter/material.dart';

class VerifikasiScreen extends StatelessWidget {
  const VerifikasiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Verifikasi")));
  }
}
